{
  "id": "sudo-network-sandbox",
  "version": "0.2.0",
  "name": "Sudo & Network Sandbox",
  "description": "Blocks sudo commands and limits network egress to only the specified domains. Useful for isolating AI agents.",
  "options": {
    "allowedDomains": {
      "description": "List of permitted domains for egress traffic (comma-separated)",
      "default": "",
      "type": "string"
    }
  },
  "capAdd": [
    "NET_ADMIN",
    "NET_RAW"
  ],
  "postStartCommand": "sudo /usr/local/bin/init-firewall.sh"
}
